
* The data you entered was saved to a remote database. 

* For edits of existing data use the `Database interface` and interact directly with the remote database.

* To view and also download the data go to `Show tables` and `Show views`. 